let itemPrice = '20'; // Price is stored as a string instead of a number
let quantity = 3;
let totalPrice = itemPrice * quantity;

console.log("final price:", totalPrice); // Expected 60, but something went wrong